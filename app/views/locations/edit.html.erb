<h1>Editing location</h1>

<% form_for(@location) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => f %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Back to Locations', locations_path %> | <%= link_to 'Home', root_url %>