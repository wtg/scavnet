<h1>New category</h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back to Categories', categories_path %> | <%= link_to 'Home', root_url %>